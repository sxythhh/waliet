# .env.example - Copy to .env and fill in values

# Supabase (client-side safe - public)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# PostHog Analytics (client-side safe - public)
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=

# Server-side only (NEVER prefix with VITE_)
# These should be set in Supabase Edge Function secrets or server environment
# SUPABASE_SERVICE_ROLE_KEY=
# ANTHROPIC_API_KEY=
# DISCORD_CLIENT_SECRET=
